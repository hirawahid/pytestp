node {
  stages {
  	stage ('SCM checkout'){
		git "https://gitlab.com/mbabilo/experitest"
		}
    stage('build') {
      steps {
        sh 'pip install -r requirements.txt'
      }
    }
    stage('test') {
      steps {
        sh 'python Tests.py'
      }   
    }
  }
}
