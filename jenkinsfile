node {
  	stage ('SCM checkout'){
		git "https://gitlab.com/mbabilo/experitest"
		}
    stage('build') {
        sh 'pip install -r requirements.txt'
    }
    stage('test') {
        sh 'python Tests.py'
    }
}
